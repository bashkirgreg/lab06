name: CI

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    
    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: sudo apt-get update && sudo apt-get install -y cmake build-essential rpm

    - name: Install dependencies (macOS)
      if: matrix.os == 'macos-latest'
      run: brew install cmake

    - name: Install WIX (Windows)
      if: matrix.os == 'windows-latest'
      run: choco install wixtoolset -y
      shell: powershell

    - name: Configure
      run: cmake -S . -B _build -DCMAKE_BUILD_TYPE=Release
      shell: bash

    - name: Build
      run: cmake --build _build --config Release
      shell: bash

    - name: Package
      run: |
        cd _build
        cpack -C Release
        ls -la
      shell: bash

    - name: Show WIX log (Windows)
      if: matrix.os == 'windows-latest' && failure()
      run: |
        if (Test-Path "D:/a/lab06/lab06/_build/_CPack_Packages/win64/WIX/wix.log") {
          Write-Host "=== WIX LOG ==="
          Get-Content "D:/a/lab06/lab06/_build/_CPack_Packages/win64/WIX/wix.log"
        } else {
          Write-Host "WIX log not found"
        }
      shell: pwsh

    - name: Upload to Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          _build/*.deb
          _build/*.rpm
          _build/*.dmg
          _build/*.msi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
